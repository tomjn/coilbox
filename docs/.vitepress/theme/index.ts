import DefaultTheme from "vitepress/theme";
import "./custom.css";

// Extends the stock VitePress theme with a small stylesheet — currently the
// homepage screenshot gallery + a dependency-free CSS lightbox (see custom.css).
export default DefaultTheme;
