/** Persisted toggle shared by the Maps and Games browse screens: when on, rows
 * whose archive is already present in a content root are filtered out of the
 * grid instead of being shown greyed-out as "Already downloaded". */
export const HIDE_INSTALLED_KEY = "downloads.hideInstalled";
