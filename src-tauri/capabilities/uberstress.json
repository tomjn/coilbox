{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "uberstress",
  "description": "Grants the uberstress plugin's default commands to the main window.",
  "windows": ["main"],
  "permissions": ["coilbox-uberstress:default"]
}
