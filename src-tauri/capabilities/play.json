{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "play",
  "description": "Grants the play plugin's start-script generation, engine launch and cancel commands to the main window, plus the save dialog used to export a start script.",
  "windows": ["main"],
  "permissions": ["coilbox-play:default", "dialog:allow-save"]
}
