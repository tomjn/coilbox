{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "profile",
  "description": "Grants the distribution-profile loader command to the main window.",
  "windows": ["main"],
  "permissions": ["coilbox-profile:default"]
}
