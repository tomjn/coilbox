{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hub",
  "description": "Grants the hub plugin's sign-in commands to the main window.",
  "windows": ["main"],
  "permissions": ["coilbox-hub:default"]
}
