{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lobby-servers",
  "description": "Grants the lobby-servers plugin's keychain credential commands to the main window.",
  "windows": ["main"],
  "permissions": ["coilbox-lobby-servers:default"]
}
