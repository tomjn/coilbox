{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "direct",
  "description": "Grants the direct-hosting plugin's room commands to the main window.",
  "windows": ["main"],
  "permissions": ["coilbox-direct:default"]
}
