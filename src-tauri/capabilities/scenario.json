{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "scenario",
  "description": "Grants the scenario plugin's storage and dialogue-media commands to the main window, plus the open dialog used to pick a portrait, voice clip or scenario file and the save dialog used to export one.",
  "windows": ["main"],
  "permissions": [
    "coilbox-scenario:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
