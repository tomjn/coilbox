"$schema" = "schemas/schema.json"

[default]
description = "Allows the workshop plugin's compiler and preflight check, which turn a saved tweak project into the Lua a game reads and check it before it leaves the app. Both read nothing and write nothing."
permissions = [
  "allow-workshop-compile",
  "allow-workshop-preflight",
]
