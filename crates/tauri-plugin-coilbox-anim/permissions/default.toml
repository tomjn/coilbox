"$schema" = "schemas/schema.json"

[default]
description = "Allows the animation plugin's commands (COB disassembly, BOS->COB compilation, reveal-in-folder)."
permissions = ["allow-anim-cob-disasm", "allow-anim-bos2cob", "allow-anim-reveal"]
