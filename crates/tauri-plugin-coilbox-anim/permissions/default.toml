"$schema" = "schemas/schema.json"

[default]
description = "Allows the animation plugin's commands (COB disassembly and BOS->COB compilation)."
permissions = ["allow-anim-cob-disasm", "allow-anim-bos2cob"]
