"$schema" = "schemas/schema.json"

[default]
description = "Allows signing in to the Coilbox hub with Discord, signing out again, and reading who is signed in."
permissions = ["allow-hub-sign-in", "allow-hub-sign-out", "allow-hub-account"]
