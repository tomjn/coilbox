"$schema" = "schemas/schema.json"

[default]
description = "Allows signing in to the Coilbox hub with Discord, signing out again, reading who is signed in, and publishing as them."
permissions = [
  "allow-hub-sign-in",
  "allow-hub-sign-out",
  "allow-hub-account",
  "allow-hub-publish",
]
