"$schema" = "schemas/schema.json"

[default]
description = "Allows starting, stopping and inspecting the lobby room this client hosts itself."
permissions = [
  "allow-direct-start-room",
  "allow-direct-stop-room",
  "allow-direct-room-status",
]
