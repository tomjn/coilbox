"$schema" = "schemas/schema.json"

[default]
description = "Allows the mapconv plugin's commands (probe, compile, decompile, cancel, settings)."
permissions = [
  "allow-mc-probe",
  "allow-mc-compile",
  "allow-mc-decompile",
  "allow-mc-cancel",
  "allow-mc-settings-load",
  "allow-mc-settings-save",
]
